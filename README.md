# Fur-img-api-next 文档

这是 Fur-img-api-next 项目的 VitePress 文档。

## 快速开始

### 安装依赖

```bash
npm install
```

### 本地开发

```bash
npm run docs:dev
```

文档将在 `http://localhost:5173` 启动。

### 构建文档

```bash
npm run docs:build
```

构建后的文档将在 `dist/` 目录中。

### 预览构建结果

```bash
npm run docs:preview
```

## 文档结构

```
docs/
├── .vitepress/
│   ├── config.ts          # VitePress 配置
│   └── theme/
│       ├── index.ts       # 自定义主题
│       └── custom.css     # 自定义样式
├── index.md               # 首页
├── guide/
│   ├── getting-started.md # 快速开始
│   ├── configuration.md   # 配置指南
│   ├── deployment.md      # 部署指南
│   └── logging.md         # 日志系统
├── api/
│   ├── overview.md        # API 概览
│   ├── random-files.md    # 随机文件 API
│   ├── file-list.md       # 文件列表 API
│   ├── status.md          # 状态检查 API
│   ├── admin.md           # 管理员接口
│   └── test.md            # API 测试工具
└── package.json           # 项目配置
```

## 功能特性

- 📖 完整的 API 文档
- 🎨 支持自动/白天/黑夜模式切换
- 🧪 集成 API 测试工具
- 📱 响应式设计
- 🔍 全文搜索
- 📝 Markdown 支持

## 主题切换

文档支持三种主题模式：

- **自动**：根据系统设置自动切换
- **白天**：浅色主题
- **黑夜**：深色主题

点击导航栏右上角的主题切换按钮即可切换。

## API 测试工具

访问 `/api/test` 页面可以使用集成的 API 测试工具，直接测试所有 API 端点。

## 部署

### 部署到 GitHub Pages

1. 在 `config.ts` 中设置 `base` 选项
2. 运行 `npm run docs:build`
3. 将 `dist/` 目录推送到 GitHub Pages

### 部署到其他平台

构建后的文档是静态 HTML，可以部署到任何支持静态网站的平台。

## 许可证

GPL-3.0-only
